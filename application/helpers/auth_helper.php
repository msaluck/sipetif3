<?php defined('BASEPATH') or exit('no direct access');

function verify_session()
{
	$CI = &get_instance();
	// if ($CI->session->userdata('status') != "login_skpi") {
	// 	redirect(site_url('login'));
	// }
}